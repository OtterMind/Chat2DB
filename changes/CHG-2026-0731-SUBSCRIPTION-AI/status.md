# Status — CHG-2026-0731-SUBSCRIPTION-AI

- Baseline commit: `44fad895098d1a03f2080860243f303087c15ef8`
- Integration branch: `feature/subscription-backed-ai` (uncommitted local implementation only)
- Overall status: onboarding UX, macOS Keychain HOME isolation, and pinned MCP tool-surface remediation implemented, verified, and installed in the local macOS preview App; subscription and secret-import release gates remain disabled pending live-provider and platform evidence
- **Model selector product decision (2026-08-01):** **Do not** reintroduce gpt-5.6-sol / luna / terra via CodeMode for the first reliable DB-tools release. Keep discovery filter for `tool_mode=code_mode_only`. Prefer gpt-5.5 (+ other function-capable models). CodeMode+sol is a **future optional track** only after direct MCP is live-proven. Decision log: epic `subscription-backed-ai-oauth/decision-log`.

## Workstreams

| Stream | Branch | Worktree | Owner | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Domain and H2 control ledger | feature/subscription-backed-ai | `/Users/chenhaijie/.traycer/worktrees/ottermind__chat2db/feature-subscription-backed-ai` | primary + Grok lanes | implemented | focused repository/state tests |
| Codex app-server, lifecycle, routing, and MCP | feature/subscription-backed-ai | same | primary + Grok lanes | implemented, default disabled | fake subprocess, lifecycle, routing, and loopback MCP tests |
| Encrypted legacy API-key import | feature/subscription-backed-ai | same | primary + Grok lanes | implemented, separately gated | crypto/JCEF/frontend migration tests |
| Renderer and packaging gates | feature/subscription-backed-ai | same | primary + Grok lanes | implemented, default disabled | frontend lint/build/tests and staging fixture |
| Independent cold code review | feature/subscription-backed-ai | same | Subscription OAuth cold-review agent | ACCEPT for code-level, default-off delivery | fourth review independently closed the final double-ledger/restart convergence blocker; live release gates remain outside this acceptance |

## Integration log

- 2026-08-01: **text2sql pressure PASS** (automated): `text2sql` → tool_ok DELEGATED + lease kept → `execute_sql` tool_ok. Tests `StreamableHttpMcpBridgeTest` + `SubscriptionMcpToolExecutorTest` **10/10**. Live out-of-turn MCP without lease correctly blocked. Decision-log row **text2sql pressure**; walkthrough updated.
- 2026-08-01: **Live verify gpt-5.5 order/query path PASS** (UTC 03:12–03:13, Preview jar `ea2999d1…`): mcp-bridge shows 7× `tool_ok`, **0** `tool_error`, **no `text2sql`** (model used schema+`execute_sql` as intended); `execute_sql` ×2 with app log `execute_sql cost time:296` / `61`; requestUserInput auto-answers `Allow`; codex `turn/completed`. No `TOOL_OUTCOME_UNKNOWN` / missing `answers`. Recorded in decision-log (**MCP tools/call E2E proven**) + walkthrough `changeset-walkthroughs/mcp-tools-call-e2e`.
- 2026-08-01: **text2sql TOOL_OUTCOME_UNKNOWN after tools/call green**: Live proof requestUserInput+Allow works; `list_all_*`/`execute_sql` were `tool_ok`. Failure was `text2sql` nested API-key `chatSync` (~85ms) → kernel fenced attempt as `TOOL_OUTCOME_UNKNOWN` and stream completed (`ai_complete`). Fix: subscription `text2sql` no longer nests AI; returns `TEXT2SQL_DELEGATED_TO_SUBSCRIPTION_MODEL` guidance (EXECUTED, no fence); developer instructions prefer schema+execute_sql. Focused tests green. Preview jar SHA-256 `ea2999d107f8e17dcb0b31151bf44b2a39d25033391f2fd3c8dd12a1a4232a6b`; preview PID 49665 / app-server PID 49713.
- 2026-08-01: **Product decision — sol not restored via CodeMode this milestone.** Options considered: (A) keep filter + gpt-5.5 for tools (chosen), (B) re-enable CodeMode + show sol now, (C) show sol disabled with “unavailable” UX only. Chose **A**. Rationale: CodeMode nested MCP already failed live; direct path + requestUserInput fix still need user verification; dual paths increase cancel risk. sol/luna/terra stay filtered until a separate CodeMode track is proven. Artifacts: decision-log rows “Product: do not put sol back…” and superseded “Reintroduce sol…”.
- 2026-08-01: **Fix requestUserInput response shape (root cause of tool cancel)**: Live codex log proved `item/tool/requestUserInput` was answered with `{decision:denied}` → `failed to deserialize ToolRequestUserInputResponse: missing field answers` → Codex fallback `answers:{}` cancels the in-flight tool (no HTTP `tools/call`). Correct shape is `{answers:{qid:{answers:[label]}}}`. Headless auto-selects affirmative/first option; secret questions skipped; MCP elicitation uses `{action:accept|decline}` not decision. Focused tests 8/8. Preview jar SHA-256 `c0e73c4b440e2ee7c4adfff44ef942d1a9dba8495e6f646a1dc80568ba5c4003`; preview PID 39880 / app-server PID 39932. **sol/luna/terra remain hidden by design** (`tool_mode=code_mode_only` filter) — use gpt-5.5. User should retry list/query; expect bridge `tools/call begin` + `tool_ok` and no deserialize error in codex logs.
- 2026-08-01: **Allow MCP FunctionCall items**: deny of `functionCall` was interruptTurn'ing gpt-5.5 `namespace=mcp__chat2db_subscription` tools → Codex `user cancelled MCP tool call`, no bridge `tools/call`. Policy allows allowlisted MCP via mcpToolCall or FunctionCall+namespace; tolerates tool_search; still fail-closed shell/exec/foreign. Preview jar SHA-256 `26a008e20433e5c0359633419f380535c6246dec898b358a54b24a4f6289a7bd`. User should retry gpt-5.5 list/query; expect `tools/call begin` + `tool_ok` in mcp-bridge.log.
- 2026-08-01: **P0/P1 tools/call hang fix**: (1) JSON-RPC client now answers app-server **serverRequest** frames (`id`+`method`) instead of dropping them — auto-approve MCP permissions, deny shell/file; (2) sandbox `workspace-write` + `network_access=true` + CLI + `CODEX_SANDBOX_NETWORK_DISABLED=0`; AlwaysDefer still forced false. Focused tests 40/40 including serverRequest auto-answer. Preview jar SHA-256 `f910dfbfad8b9885d070fc9951ec1fed020d83a9c91d90d9cbee35f40a186943`; live argv includes workspace-write/network_access overrides; config regenerated. User should retry gpt-5.5 list/query and check `mcp-bridge.log` for `tools/call begin` + `tool_ok`.
- 2026-08-01: **Disabled CodeMode nested MCP**; force direct function/MCP HTTP `tools/call`. config + CLI: `code_mode=false`, `code_mode_host=false`, `code_mode_only=false`, tool_search flags still false; no `CODEX_CODE_MODE_HOST_PATH`; deny all `customToolCall`/`exec`; developer instructions require direct `chat2db_subscription` MCP. Compatibility policy v2 filters `tool_mode=code_mode_only` and known `gpt-5.6-luna/sol/terra` ids. Focused tests 54/54. Preview reinstalled jar SHA-256 `e07d3e0c6254c5907e8b2c744569bb9d7d1e31629adf1f407bbffe43fa926a92`; app-server PID shows the new `-c` overrides; regenerated config.toml has `code_mode=false`. User should refresh models, pick a non-5.6-code_mode_only model (e.g. gpt-5.5), list/query, then check `~/.chat2db-community/logs/mcp-bridge.log` for `tools/call begin` + tool_ok.
- 2026-08-01: Forced MCP off code-mode dispatch: `direct_only_tool_namespaces` now includes **`mcp__chat2db_subscription`** (Codex real namespace); app-server launch adds CLI `-c` overrides for tool_search defer off + direct_only; logback-desktop → `~/.chat2db-community/logs`; dual-write `mcp-bridge.log`. Preview jar `7d9dbdd02ba9917d793d8d351e6a5e9d0c7833ab0f953d1f5d42f7913e632764`; live argv shows the `-c` overrides; config.toml has the new namespaces. User should retry list/query and check `~/.chat2db-community/logs/mcp-bridge.log` for `tools/call begin`.
- 2026-08-01: Nested MCP after list still hung (~6s) and Chat2DB interrupt killed the turn with no tools/call. Aligned official **0.144.6** `codex-code-mode-host` (SHA `de329ec247b5ebbdf796b5888a7c2a9d731e221321584c5abdcc686c70b2db81`) into packaging manifest + stage script; copy host to space-free `CODEX_HOME/bin` when the `.app` path has spaces; added MCP bridge diagnostics (`subscription mcp request/tools/call begin`) without logging secrets/args/bodies. Preview reinstalled jar `ed252b08fa3e506c4701960142e12a76463d516c42e248b2ac6e8304122d58c9`, host pin in runtime.properties, preview PID 60070 / app-server 60118. User should retry list/query; logs should show either tools/call begin or only tools/list then stall.
- 2026-08-01: After tool-search flags, user still saw `APP_SERVER_CODE_MODE_NOT_ALLOWED` (“订阅 AI 仅允许…不允许 exec/code-mode/shell”). Live model catalog for `gpt-5.6-luna`/`sol` has `tool_mode=code_mode_only`, so the model **must** use `custom_tool_call` name `exec` + nested `tools.mcp__…`. Rejecting all exec items blocked every database tool on 5.6. Fix: enable `code_mode=true` + `code_mode_host=true`, allow the `exec` transport and nested allowlisted `mcpToolCall` (including `call_id=exec-…`), deny shell/file/web, resolve `CODEX_CODE_MODE_HOST_PATH` (sibling/PATH), stage `codex-code-mode-host` next to app-server for Preview. Focused tests pass; Preview jar SHA-256 `4827ed50eb65cd3f92365c77d0767d5dd526fb526dc94fe8bf1a8f771e652bb7`; preview PID 53199 / app-server PID 53254; config shows `code_mode=true` / `code_mode_host=true`. Residual: host binary currently staged from local 0.145.0 package against app-server 0.144.6 — prefer same-version host in formal packaging.
- 2026-08-01: Root-caused `APP_SERVER_MCP_STALLED` / “数据库工具未能及时启动（常见于 code-mode 包装调用）”. Live Codex features included default-on `ToolSearchAlwaysDeferMcpTools`; model used `custom_tool_call` `exec` with nested MCP, hung without host. First fix forced tool-search off; insufficient alone for GPT-5.6 `code_mode_only` catalog.
- 2026-07-31: Traced a reply that emitted visible text and then failed with `APP_SERVER_TOOL_NOT_ALLOWED` to pinned Codex v0.144.6's built-in `read_mcp_resource` / `list_mcp_resources` metadata helpers. They are injected whenever a direct MCP server is present even though Chat2DB exposes only database tools.
- 2026-07-31: Restricted the generated app-server MCP registration to the seven Chat2DB database tools with `enabled_tools`, added per-thread instructions that prohibit resource discovery, and made the isolated bridge advertise empty resource/resource-template catalogs. Only the pinned side-effect-free metadata events are tolerated; command execution, file changes, foreign MCP servers, and unknown tools remain fail-closed.
- 2026-07-31: Rebuilt and installed the refreshed backend into `/Applications/Chat2DB Subscription Preview.app`, re-signed with the existing local Apple Development identity, and launched preview PID 58200 with pinned app-server PID 58260. Source/install Jar SHA-256 is `4ad136d9f937214a3f497aa66debdd63a282c3bd4f592ad9b80499f59918d8ce`; the generated config contains the exact database allowlist and no capability value.
- 2026-07-31: Traced a completed-but-empty renderer reply end to end. Pinned app-server emitted `item/agentMessage/delta` and `turn/completed`; the H2 attempt was `COMPLETED` with one persisted two-character answer, proving the loss was after backend output persistence at the independently scheduled JCEF push boundary.
- 2026-07-31: Added an idempotent terminal answer recovery envelope. Subscription `done` events now carry the complete final answer; the renderer restores an entirely missed answer or only a missing suffix, never duplicates a complete stream, and never overwrites conflicting visible text. Streaming refs are updated synchronously before terminal handling.
- 2026-07-31: Rebuilt and reinstalled the Preview Jar and Community renderer, re-signed without hardened-runtime Team-ID mismatch, and restarted preview PID 40186 with pinned app-server child PID 40265. A subsequent user-initiated live turn completed with 11 persisted deltas / 15 characters; installed/source Jar and renderer hashes match. Visible renderer acceptance still requires the user's confirmation.
- 2026-07-31: Corrected the earlier restart acceptance claim after live UI showed only a persisted `GPT-5.6-Sol` label and no subscription options. The backend ledger was healthy (`OPENAI=CONNECTED`, seven available snapshots); the actual defect was the renderer keeping a separate model-option map that was not rebuilt when ordinary startup/dropdown refresh changed snapshots from stale to selectable.
- 2026-07-31: Made every stale-to-selectable subscription snapshot transition reload the AI model-option map, independent of the post-login guidance flag. Added a bounded startup retry for the specific `CONNECTED` + no-selectable-model window while `ApplicationReadyEvent` restores the authenticated catalog; the retry never starts OAuth.
- 2026-07-31: Rebuilt and reinstalled only the Preview renderer, re-signed the App, and restarted without login. Preview PID 28342 launched app-server PID 28411; startup logs show the expected delayed second capability/provider/model refresh, one authenticated `model/list`, successful Keyring load, and zero `account/login/start` calls. Installed/source renderer hash is `c7e5ce7a4ef866f7b11b957a7a025be61c22ba79f63ff80841ec9e01fc2bd17c`.
- 2026-07-31: Serialized ordinary AI startup so subscription capability/provider/model hydration completes before rebuilding the selectable model map; component teardown cancels a late option load, while the post-login guidance path remains unchanged.
- 2026-07-31: Rebuilt and installed the Community renderer, re-signed and restarted the Preview without OAuth. Preview PID 19000 launched app-server PID 19092, Keyring load succeeded, eight models were restored, no `account/login/start` occurred, and the persisted subscription selection remained in JCEF storage.
- 2026-07-31: Startup-invalid-model diagnosis found no credential loss: the installed app logged a successful Keyring load and an authenticated eight-model catalog refresh, while JCEF local storage retained the selected subscription model. The renderer independently started model-option loading before subscription surface hydration and never reloaded on ordinary startup; re-login only appeared necessary because its post-login effect happened to rebuild the option map.
- 2026-07-31: Traced a user-initiated empty reply to the exact app-server boundary: local history persisted the user message and pinned v0.144.6 received `thread/start`, but no `turn/start` followed because Chat2DB sent the invalid sandbox enum `readOnly`.
- 2026-07-31: Corrected `thread/start.sandbox` to the v0.144.6 wire value `read-only`, added a request-capture regression, proved RED (`expected read-only, actual readOnly`) then GREEN, and independently proved the real packaged binary returns `thread/started` for the corrected request.
- 2026-07-31: Repackaged and installed `/Applications/Chat2DB Subscription Preview.app`; source/install Jar SHA-256 is `d617970c7de99004f0c24d424a7401365ae872a8015a821431809bc3e3c5ed7f`, preview PID 4551 launched pinned app-server PID 4589, and the formal Community App remained stopped.
- 2026-07-31: Fixed silent subscription replies by forwarding application-level SSE `error` chunks into the chat state instead of consuming them as generic transport errors; removed raw prompt/chunk console diagnostics.
- 2026-07-31: Added end-to-end reasoning-effort discovery, schema-v3 persistence, controller/renderer propagation, a compact model-adjacent selector, backend validation, and `turn/start.effort`; High is selected when the provider advertises it.
- 2026-07-31: Closed two startup races: failed legacy-snapshot capability refreshes remain retryable, and already-connected Keyring accounts refresh the authenticated model catalog after the app-server becomes ready.
- 2026-07-31: Rebuilt and reinstalled `/Applications/Chat2DB Subscription Preview.app`; installed/source Jar SHA-256 is `9816b117a39fbafbdd22c0d655b11dd5fdb6b945770620af19b07e9dee63495`. Preview PID 82552 launched pinned app-server PID 82585; the formal Community App remained stopped.
- 2026-07-31: Read-only installed-state evidence confirms the provider catalog now contains real reasoning capability rows: GPT-5.4 advertises `low,medium,high,xhigh`, and GPT-5.6 Sol/Terra also advertise `max,ultra`.
- 2026-07-31: Fixed the post-OAuth status projection deadlock: `account/login/completed` no longer performs synchronous `account/read` on `codex-app-server-jsonrpc-reader`; login completion is serialized on a dedicated daemon executor that is shut down with the runtime.
- 2026-07-31: Added startup reconciliation for a Keyring-authenticated account when the durable provider projection is `DISCONNECTED` or orphaned `CONNECTING`; successful reconciliation restores the masked connection, refreshes the model snapshot, and never chooses a default model.
- 2026-07-31: Rebuilt and reinstalled `/Applications/Chat2DB Subscription Preview.app`, re-signed it with the existing local Apple Development identity, and launched preview PID 28710 with app-server PID 28746. The installed/source Jar SHA-256 is `a5bb16a001368ca430b322426cf27098385b43327ae719439081b394e3eefb82`; the formal Community App remained stopped.
- 2026-07-31: Fixed the macOS OAuth persistence root cause: the app-server now keeps an isolated `CODEX_HOME` but inherits the real OS-user `HOME`, allowing Security.framework to resolve the login Keychain without exposing provider credential variables.
- 2026-07-31: Upgraded macOS Keyring preflight from `/usr/bin/security` presence to a bounded, output-discarding `security default-keychain -d user` lookup; failures and timeouts remain fail-closed and file fallback remains forbidden.
- 2026-07-31: Rebuilt renderer and backend, restaged the SHA-pinned official v0.144.6 app-server, and installed the refreshed payload into `/Applications/Chat2DB Subscription Preview.app`; preview PID 8691 launched app-server PID 8741, while the formal Community App remained stopped and untouched.
- 2026-07-31: Fixed packaged macOS manifest discovery by deriving `Contents/app` from the trusted jpackage launcher path, with normalized containment that rejects traversal outside the payload directory.
- 2026-07-31: Replaced exception-class-only startup diagnostics with stable stage/reason codes; startup logs never include exception messages, paths, URLs, arguments, stderr, credentials, or provider payloads.
- 2026-07-31: Rebuilt and reinstalled the local preview payload, re-signed it, and verified PID 63919 launched pinned `codex-app-server` child PID 63976 from `user.dir=/`; the runtime directory exists, the only listener is loopback, no `auth.json` exists, and the generated config contains no capability value.
- 2026-07-31: Completed onboarding remediation: the settings entry now uses direct ChatGPT subscription wording and three-step guidance, recoverable capability/keyring/app-server failures stay visible with safe localized retry, and the actual AI model selector exposes connect/recovery actions while preserving API-key models.
- 2026-07-31: Fenced post-login guidance to a renderer-observed ChatGPT connection attempt, so ordinary startup snapshot hydration cannot unexpectedly open the model selector.
- 2026-07-31: Rebuilt the Community renderer with Node 18.20.8 and backend with Java 17, passed 98 fresh start-module subscription/secret-import tests, and installed identical renderer/Jar/lib payloads into `/Applications/Chat2DB Subscription Preview.app` version `5.3.0.731`.
- 2026-07-31: Re-signed the preview with the existing local Apple Development identity, verified the deep signature and Community/OFFLINE/loopback/preview flags, restarted it as PID 9320, and left the concurrently running formal Community App PID 57256 untouched.
- 2026-07-31: Installed-preview acceptance found that the real AI model selector did not consume the existing quick-connect grouping and that recoverable capability failures could hide the settings entry; direct onboarding, retry, and post-discovery guidance remediation started.
- 2026-07-31: Cold artifact critique returned BLOCK.
- 2026-07-31: Product and architecture revised; provider support, credentials, discovery, migration, retry, logout, concurrency, and residual DML/DDL risk settled.
- 2026-07-31: Implemented ChatGPT subscription support through the pinned Codex app-server contract; SuperGrok remains explicitly ineligible.
- 2026-07-31: Added H2 control-plane state, crash-recoverable logout, durable attempt/tool journals, loopback capability MCP, and encrypted early JCEF import.
- 2026-07-31: Preserved the existing API-key route and made all subscription/import release flags default-off.
- 2026-07-31: Removed subscription tool calls from the legacy exception-logging/result-text path; strict calls now feed only the safe outcome state machine.
- 2026-07-31: First independent code cold review returned BLOCK with eight findings.
- 2026-07-31: Moved app-server turns to an empty supervisor-owned cwd, disabled every pinned native exec/browser/plugin feature, and added fail-closed item filtering.
- 2026-07-31: Reconciled the deny list against the official v0.144.6 feature table and explicitly disabled the remaining default-on `shell_snapshot`, `code_mode_host`, and `workspace_dependencies` surfaces.
- 2026-07-31: Added restart recovery for orphan attempts/leases, durable tool/logout fencing, retryable disconnect-failed projection, atomic busy-send reservation, stale/rejected model invalidation, and renderer payload-log minimization.
- 2026-07-31: Connected encrypted legacy import to a versioned two-phase H2 ledger with stable renderer item IDs, cross-restart acknowledgements, and terminal unknown outcomes that cannot replay.
- 2026-07-31: Second independent code cold review returned BLOCK on owning-stream fencing, double-ledger-failure handling, secret response fragments, and future-schema compatibility.
- 2026-07-31: Made sign-out atomically terminalize the leased attempt/tool, added in-process owning-context fences and authoritative late-event rejection, post-tool lease recheck, unconditional MCP fail-closed handling, metadata-only secret acknowledgements, and a pre-DDL future-schema guard.
- 2026-07-31: Third independent code cold review closed owning-stream fencing, secret-fragment responses, and future-schema handling, but retained BLOCK because a double-ledger failure could persist `attempt=INTERRUPTED` beside `tool=OUTCOME_UNKNOWN` after restart.
- 2026-07-31: Made requested tool-unknown fences dominate stale non-terminal ledger reads and made startup recovery force every attempt with a `STARTED`/`OUTCOME_UNKNOWN` tool record to `TOOL_OUTCOME_UNKNOWN`, including previously terminalized inconsistent rows.
- 2026-07-31: Fourth independent code cold review returned ACCEPT for code-level, default-off delivery; no release flag was enabled and no live-provider/platform claim was made.

## Regression results

- Pinned MCP tool-surface remediation: explicit RED produced four failures for missing `enabled_tools`, missing thread guidance, output-then-resource false denial, and missing empty resource catalogs. GREEN executes 32 focused app-server/stream/MCP tests and 110 complete start-module subscription tests with 0 failures/errors/skips; the build-after-package focused rerun also executes 32 tests with 0 failures/errors/skips.
- Refreshed tool-policy preview: Java 17 clean package passed with tests skipped by design after the separate enabled runs; deep code-sign verification passed, source/install Jar hashes match, preview/app-server ancestry is healthy, and the generated app-server config contains only the seven database tools.
- Terminal answer recovery: explicit frontend RED failed on the missing recovery helper and backend RED failed on the missing terminal payload contract; GREEN covers missed, partial, complete, and conflicting terminal answers. Renderer subscription/secret-import tests, full ESLint/Stylelint, the Node 18.20.8 Community build, and 9 focused backend stream tests passed with zero failures/errors/skips.
- Refreshed chat preview: Java 17 clean package passed with tests skipped by design after the separate enabled run; deep signature verification and installed/source Jar/renderer hashes passed. Preview PID 40186 and app-server PID 40265 are healthy; the live post-install turn reached `COMPLETED` with 11 output rows / 15 characters in the durable ledger.
- Cold-start selectable-model recovery: explicit RED proved the ordinary recovery path did not retry/reload; GREEN covers bounded no-login startup retry, stale-to-selectable option reload, post-login-only guidance, and unmount cancellation. Subscription/secret-import flows, model-select regression, full ESLint/Stylelint, and the Node 18.20.8 Community build passed. The i18n source-hash validator still reports the pre-existing es-ES/ko-KR drift and was not altered in this fix.
- Restart model-restoration remediation: explicit RED proved model-option loading did not wait for subscription hydration; GREEN covers ordering and unmount cancellation. Node 18.20.8 subscription/secret-import tests, full ESLint/Stylelint, and the Community build all passed.
- No-login installed restart: source and installed renderer trees match; `p__main__CommunityMainPage.async.js` SHA-256 is `9237d1faec11af1e1290b5150cf5846686ac0ca30bd74176f6c1dfc7fc6f33d5`, deep signature verification passed, Keyring/model discovery succeeded, and the post-restart login-start count was zero.
- Pinned thread-start protocol remediation: explicit RED reproduced `readOnly` versus `read-only`; focused supervisor tests executed 10 cases and the complete start-module subscription suite executed 107 cases with 0 failures/errors/skips. A credential-free real v0.144.6 probe rejected `readOnly` with JSON-RPC `-32600` and immediately accepted `read-only` with `thread/started`.
- Refreshed thread-start preview: Java 17 package passed with tests skipped by design after the separate enabled test run; deep signature verification passed, source/install Jar hashes match at `d617970c7de99004f0c24d424a7401365ae872a8015a821431809bc3e3c5ed7f`, and authenticated startup restored the Keyring account plus eight cached models.
- Chat stream/reasoning effort remediation: renderer subscription flows, secret-import transport, touched ESLint, full ESLint/Stylelint, and the Node 18.20.8 Community build passed; Java 17 subscription tests executed 107 start-module tests plus 21 H2 tests with 0 failures/errors/skips.
- Startup-race remediation: explicit RED tests reproduced both the failed-refresh latch and the already-connected catalog omission; focused runtime/lifecycle tests now execute 26 cases with 0 failures/errors/skips.
- Current preview: Java 17 package passed with tests skipped by design after the separate enabled test run; deep signature verification passed, preview/app-server ancestry is healthy, installed/source Jar hashes match, no `auth.json` exists, and the only observed listener is loopback.
- Login completion/status remediation: explicit RED `TimeoutException` proved the JSON-RPC reader callback blocked on account confirmation; the corrected runtime/lifecycle focused run executed 24 tests and the complete start-module subscription run executed 104 tests, all with 0 failures/errors/skips.
- Installed login-status preview: source and installed Jar hashes match at `a5bb16a001368ca430b322426cf27098385b43327ae719439081b394e3eefb82`; deep code-sign verification passes and the pinned app-server child launches. Startup did not observe a reusable authenticated account, so one user-initiated Connect remains required to validate the repaired live callback and model discovery.
- macOS HOME/Keychain remediation: explicit RED test-compilation failure for the new injectable Keychain contract, followed by 13 focused app-server/Keychain tests and 102 full start-module subscription tests with 0 failures/errors/skips.
- Refreshed Keychain preview: Node 18.20.8 renderer build and Java 17 backend package passed; source/install Jar SHA-256 is `05a753ca8b25ebba72f4978749d0e4de02bfa583a4eb1056908c183c85889845`, all 326 libraries and renderer files match, and the installed official app-server SHA-256 matches staging at `94fe94f16ae2c02afacad54eea1d04c2a9d64d9116131c535fdc72f297303fe5`.
- Installed macOS Keychain evidence: the real-HOME/isolated-`CODEX_HOME` lookup resolves `login.keychain-db`, generated config remains `keyring`-only, no `auth.json` exists, deep code-sign verification passes, and the only preview listener observed is loopback `127.0.0.1`.
- Packaged runtime remediation: explicit RED compilation failure on the missing macOS resolver/startup-stage API, followed by 8 focused runtime/manifest tests and 101 start-module subscription/secret-import tests with 0 failures/errors/skips.
- Fresh preview refresh: Community renderer rebuilt with verified Node 18.20.8; Java 17 clean package passed with tests skipped by design; the 101-test suite was rerun after packaging; installed Jar SHA-256 is `f138966d73b3662e1e2d0920d3cb8abea8a208f5f253d25f637c36d23122eefa` and installed Jar/lib/dist match the build outputs.
- UX remediation focused evidence: subscription/secret-import renderer flow tests, existing model-select tests, and i18n source validation all passed after an explicit RED-to-GREEN cycle.
- UX remediation frontend evidence: full ESLint/Stylelint passed; Community Umi build passed with Node 18.20.8 and webpack compiled successfully in 21.60 seconds.
- Fresh preview backend evidence: Java 17 clean package passed with tests skipped by design; the subsequent explicitly enabled subscription/secret-import run executed 98 tests with 0 failures/errors/skips, including 5 `AiSubscriptionControllerTest` cases.
- Installed preview evidence: source and installed backend jar SHA-256 both `cc2875eea06c410e96949a73c212ec348c7126f380c257320c161ea5f6bea38f`; renderer `dist` and all 326 runtime libraries matched by recursive diff; deep code-sign verification passed.
- Backend subscription suites: 149 tests, 0 failures/errors/skips (Tools 20, Domain API 5, Storage 21, JCEF 6, Start 97).
- Adjacent encrypted model-config storage regression: 14 tests, 0 failures/errors.
- Final retained Surefire evidence: 27 XML reports / 163 tests / 0 failures/errors/skips after the clean package and test reruns.
- Strict subscription MCP adapter and tool outcome path: 6 tests, 0 failures/errors.
- Community backend package: passed with tests explicitly skipped for the packaging command; focused tests above are the test evidence.
- Frontend subscription flow and existing model-select regression: passed.
- Full frontend ESLint, Stylelint, subscription/secret-import/canary tests, and Community Umi build: passed under task-local Node 18.20.8.
- Default-off Community loopback smoke: HTTP root served, subscription controller had no mapping, listener was `127.0.0.1` only, and the task-owned process was stopped.
- Staging/checksum fixture and touched shell syntax checks: passed, including the expected negative checksum case. The official macOS arm64 v0.144.6 asset also staged with matching manifest/license checks and reported `codex-app-server 0.144.6`.

## Remaining risks

- The repaired output-then-resource path is covered by fake app-server and real installed configuration evidence; one user-initiated post-install chat remains required to confirm the live provider no longer surfaces `APP_SERVER_TOOL_NOT_ALLOWED`.
- Browser OAuth, persisted account projection, authenticated model discovery, no-login restart, and a post-install live text turn are now observed locally; the remaining acceptance point is visible renderer confirmation that the terminal recovery envelope displays the answer.
- The current macOS preview proves the packaged manifest/startup path only; Windows and Linux jpackage launcher layouts still require their target-platform installer runs before release enablement.
- Automated macOS GUI clicking/screenshots remain unavailable in the current harness because the Computer Use native pipe is not exposed; installation/runtime evidence is process-, bundle-, and artifact-level.
- Provider remains disabled until pinned binary, per-platform packaging, native-tool isolation, and real-account evidence pass.
- Automatic DML/DDL has an irreducible external database outcome-unknown window.
- Full user-profile/same-user compromise is outside local file encryption protection.
- No live ChatGPT turn/tool/logout completion or target-platform signed native installer has been exercised yet.

## Memory and documentation updates

No durable memory update requested. Planning artifacts and this change pack are the authoritative records.

## 2026-07-31 idle event watchdog

- Added 90s idle event watchdog after partial subscription stream output; preserves partial answer, releases provider lease, error `TURN_IDLE_TIMEOUT_OUTCOME_UNKNOWN`.
- Allowed `ACTIVE`/`OUTPUT_VISIBLE` -> `OUTCOME_UNKNOWN` transitions.
- Focused stream tests 12/12; subscription module 112/112; contracts 4/4.
- Preview reinstalled jar SHA-256 `a984a0e737be4ef9bf71f8ced0ec0876e368f6b4da2854e2da953c588a8b2bd5`; formal Community jar unchanged.

## 2026-08-01 idle UX follow-up

- Diagnosed: idle watchdog worked (attempt 5f9863db OUTCOME_UNKNOWN, lease cleared, ai_complete sent).
- Provider still stops mid-stream; Chat2DB now ends after 45s silence with readable error; SSE keeps listener until done for finalAnswer.
- Reinstalled Preview jar `f81722203f8f3de1d0ead4f87407502bb08f343879172f5a85f7e09ebba867e0` + new renderer.

## 2026-08-01 API tool parity for subscription path

- Fail-closed on exec/code-mode/customToolCall (`APP_SERVER_CODE_MODE_NOT_ALLOWED`).
- Direct MCP start watchdog 12s (`APP_SERVER_MCP_STALLED`) when kernel never journals.
- Developer instructions: same 7 tools as API-key path; no create datasource; UI for connections.
- config: `skills = false` plus existing code_mode_host=false.
- Preview jar SHA-256 `fa0fe890d19052c41bdd7c9936268568ee8e77f2de8ac43036ccdb8eee6e43c9`.
