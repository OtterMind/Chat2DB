export const WORKSPACE_TREE_TOOLBAR_BUTTON_SIZE = {
  boxSize: 24,
  iconSize: 16,
  borderRadius: 4,
  strokeWidth: 2,
} as const;

export const WORKSPACE_TREE_TOOLBAR_SECONDARY_BUTTON_SIZE = {
  ...WORKSPACE_TREE_TOOLBAR_BUTTON_SIZE,
  iconSize: 18,
} as const;
